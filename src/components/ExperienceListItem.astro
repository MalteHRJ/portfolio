---
const { title, description, thumbnail, href } = Astro.props;
---

   <a href={href} class="rounded-lg hover:outline-4 hover:outline-dark dark:hover:outline-light hover:outline  transition-all">
    <article class="grid place-items-center  sm:grid-cols-[3fr_1fr] gap-2 p-4 border border-dark dark:border-light  max-w-screen-sm rounded-lg">
      <div class="grid gap-2">
        <h2 class="text-xl">{title}</h2>
        <p class="text-base">{description}</p>
      </div>
      <img src={thumbnail} alt=""  class=" hidden sm:block object-cover border border-dark dark:border-light"/>
   
    </article>
</a>